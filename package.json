{
  "name": "hermes-claude-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Minimal stdio bridge between Hermes Agent and Claude Code",
  "main": "src/bridge.js",
  "scripts": {
    "start": "node src/bridge.js",
    "test": "echo 'ok'",
    "build": "node build.mjs",
    "postinstall": "node scripts/setup-mcp.cjs",
    "prepublishOnly": "npm run build",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.111.0",
    "axios": "^1.18.1",
    "chalk": "^5.6.2",
    "get-east-asian-width": "^1.6.0",
    "lodash-es": "^4.18.1",
    "qrcode": "^1.5.4",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "bin": {
    "hbridge": "dist/hbridge.mjs"
  }
}